/**
 * The one typed list of permissions (I18): each kind of thing, and the actions
 * allowed on it. Server, client and tests import it. Empty until stage 1.
 */
export const permissions: Readonly<Record<string, readonly string[]>> = {};
